# Verifier audit — cheap-drift worker

Branch: `orch/model-discovery/cheap-drift`
Verifier ran read-only reproduction; no target source files modified.

## Commands run and outcomes

- `python3 -m pytest tests/test_cheap_drift_check.py -v` → 3 passed
  (no_drift exit0, drift_over_threshold non-zero, offline --catalog-file + dot/dash normalization).
- `make drift-cheap` → executed checker, total_drift_count=0, exit 0.
- Manual drifted catalog (missing `claude-sonnet-4-6`, extra `extra-model`, drift=2):
  - default threshold 0 → exit 1 (non-zero) ✓
  - threshold 5 → exit 0 (within) ✓
  - threshold 2 (== drift) → exit 0 (`within_threshold` uses `<=`) ✓
  - threshold 1 → exit 1 (over) ✓
- `rg -n 'chat/completions|max_tokens|completions|messages|prompt' scripts/cheap_drift_check.py` → no matches.
  Only HTTP call in script is `GET {CLIPROXY_URL}/v1/models` (catalog listing, zero token cost).
- `git diff --name-only d4a621b HEAD` → Makefile, scripts/cheap_drift_check.py, tests/*; no `services/**` changes.
- PR #368 → OPEN, draft, base `main`, head `orch/model-discovery/cheap-drift`.

## Notes
- pytest was not preinstalled; installed via pip to run the suite.
- JSON report includes `configured_not_served` and `served_not_configured` plus counts/threshold/sources.
- Alias normalization (lowercase + `.`→`-`) verified: config `gpt-5-4` matches served `gpt-5.4` with no drift.
