# CI runner requirements for this repo are declared in:
#   requirements/ci-runner.manifest.yaml
#
# Aggregated and applied to self-hosted runners via homelab-gitops:
#   cd ~/repos/homelab-gitops && make sync-ci-runners && make apply-ci-runners
#
# See homelab-gitops/docs/CI_RUNNER_SETUP.md
