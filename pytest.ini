[pytest]
asyncio_mode = auto
markers =
    integration: real-provider E2E against a running gateway
    mock: runs against the canned mock upstream (no OAuth / no real LLM)
    smoke: minimal happy-path subset for the gated real-provider tier
