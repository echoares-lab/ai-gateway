Create a new file at exactly this path: utils/format_currency.py

It must define a Python function with this exact signature:

def format_currency(cents: int) -> str:

The function takes an integer number of cents and returns a string formatted
as US dollars with a leading "$" and exactly two decimal places, using a
comma as the thousands separator, e.g.:

format_currency(1050) -> "$10.50"
format_currency(100000) -> "$1,000.00"
format_currency(5) -> "$0.05"

Only include that one function in the file (plus any needed imports). Do not
create any other files. Use your file-writing tool to create the file
directly on disk. Do not add any explanation, just create the file.
